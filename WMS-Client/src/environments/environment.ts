export const environment = {
  // Local development environment settings.
  // Route API requests through the Angular dev-server proxy in development.
  // The proxy forwards /api to the local API host.
  production: false,
  apiUrl: '/api'
};